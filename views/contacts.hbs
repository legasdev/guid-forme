<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <meta charset="utf-8" />
</head>
<body>
   	{{> menu}}
    <h1>{{title}}</h1>
    <ul>
    	{{#each email}}
    	<li>Электронный адрес: {{this}}</li>
    	{{/each}}
    </ul>
    <p>Телефон: {{tel}}</p>
    
    <p>{{getDate}}</p>
    {{> footer}}
</body>
<html>